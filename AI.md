# مواصفات معمارية وتوزيع كوتا Gemini API لمشروع الحاسبة والـ Agent الذكي

---

## 1. الملاحظات الأساسية وإدارة النماذج

> **تنبيه هام حول حالة النماذج:**  
> جدول النماذج القديم لا يمكن اعتماده حرفياً، حيث تم **إيقاف Gemini 2.0 Flash رسمياً في 1 يونيو 2026**. كما أن نماذج Gemini 1.5 و Gemini 1.0 أصبحت قديمة ولم تعد مناسبة كبدائل احتياطية (Fallbacks).

### حالة النماذج الحالية:
* **نماذج مستقرة مستمرة (موصى بها):** `gemini-2.5-pro`, `gemini-2.5-flash`, `gemini-2.5-flash-lite` (متاحة حالياً دون تاريخ إيقاف معلن).
* **البدائل الرسمية الحديثة:** `gemini-3.6-flash`, `gemini-3.5-flash-lite`.
* **ملاحظة الكوتا (Quota Rules):** حدود (RPM / TPM / RPD) تطبق على مستوى **المشروع كاملاً** وليس على مستوى مفتاح API منفرداً، وقد تختلف الأرقام الحقيقية عن الأرقام المنشورة بناءً على مستوى الحساب. يجب قراءة الحدود من **Google AI Studio** وجعلها إعدادات قابلة للتغيير ديناميكياً (Environment Variables / Config) بدلاً من كتابتها كأرقام ثابتة داخل الكود.

---

## 2. استراتيجية توزيع النماذج (Model Routing Strategy)

| المهمة | النموذج الأساسي | النموذج البديل عند نفاد الحصة / الخطأ (429) |
| :--- | :--- | :--- |
| **تحديد نية المستخدم واختيار الأدوات** | `gemini-2.5-flash-lite` | `gemini-3.5-flash-lite` |
| **المحادثة العادية والعمليات الحسابية والرسم البياني** | `gemini-2.5-flash` | `gemini-3.6-flash` |
| **كتابة وتعديل كود Python** | `gemini-2.5-flash` | `gemini-3.6-flash` |
| **المسائل المعقدة ومراجعة الأكواد البرمجية** | `gemini-2.5-pro` | نموذج Pro يحدد ديناميكياً من الإعدادات |

> **قاعدة الانتقال (Fallback Policy):**  
> لا ينتقل النظام إلى نموذج آخر عند كل خطأ عابر. يتم الانتقال فقط بعد التأكد من أن الخطأ هو **429 (Too Many Requests / Quota Exceeded)** أو أن النموذج الحالي غير متاح (Service Unavailable).

---

## 3. مخطط المعمارية البرمجية (System Architecture)

```text
[ واجهة المستخدم (UI) ]
         ↓
[ Client Agent Orchestrator ]
         ↓ (سياق مصغر Minimised Context)
[ API Route: /api/agent/turn ]
         ↓
[ Model Router + Quota Manager ]
         ↓
[ Gemini API (Google GenAI SDK) ]
         ↓ (Function Call Proposal)
[ Client Tool Registry ]
         ↓ (بعد التحقق والموافقة)
[ Zustand / Desmos / Pyodide / Calculator Modes ]
```

### إعدادات البيئة والمكتبات:
* **الأمان:** مفتاح `GEMINI_API_KEY` يوضع في ملف `.env` ويبقى على الخادم (Server-side) فقط، **ولا يحمل** بادئة `NEXT_PUBLIC_`.
* **الحزم الرسمية:**
```bash
pnpm add @google/genai zod @xyflow/react
```
* يُستخدم `@google/genai` باعتباره الـ SDK الرسمي الحديث والقياسي من قوقل بدلاً من الحزم القديمة.

---

## 4. أدوات الـ Agent (Tool Registry)

> **قاعدة أمان:** لن يُسمح للنموذج بالوصول المباشر إلى Zustand أو نظام الملفات. يكتفي النموذج باقتراح **Function Call**، ويقوم التطبيق بالتحقق من صحتها ثم تنفيذها.

### أ) أدوات الحاسبة (Calculator Tools)
* `get_calculator_state`: قراءة حالة الحاسبة الحالية.
* `set_calculator_mode`: تغيير وضع الحاسبة.
* `set_expression`: كتابة تعبير حساسي.
* `evaluate_expression`: حساب التعبير الحسابي.
* `set_angle_unit`: ضبط وحدة الزاوية (Rad/Deg).
* `read_answer`: قراءة الناتج الأخير (`Ans`).
* `read_variables`: قراءة المتغيرات المسجلة.
* `store_variable`: حفظ قيمة في متغير.
* `open_main_menu`: فتح القائمة الرئيسية.

### ب) أدوات الرسم البياني (Graph Tools)
* `list_graph_equations` / `add_graph_equation` / `update_graph_equation` / `remove_graph_equation` / `toggle_graph_equation`
* `set_graph_window`: ضبط حدود محاور الرسم البياني.
* `zoom_graph` / `trace_graph`
* `find_intersection` / `find_root`

*مثال:* عند طلب *"ارسم sin(x) وحدد نقاط التقاطع مع x/2"*، ينتج الـ Agent معادلتين وأوامر رسم وتتبع منظمة بدلاً من إرسال نص عشوائي إلى Desmos.

### ج) أدوات بيئة Python
* `list_python_files` / `read_python_file` / `create_python_file` / `rename_python_file` / `update_python_file` / `delete_python_file`
* `run_python_file` / `stop_python_execution` / `read_python_output`

> **ضوابط:** التعديل يُعرض على المستخدم كـ **Diff** قبل التطبيق. عمليات الحذف والاستبدال وتشغيل الأكواد المولدة تتطلب **موافقة صريحة (Human Approval)**.

### د) الأدوات التخصصية الأخرى
* **Matrix:** العمليات الأساسية، المحدد (Determinant)، المعكوس، والرتبة (Rank).
* **Vector:** حساب المقدار، الضرب القياسي والمتجهي، والزوايا.
* **Statistics:** تحليل البيانات، الانحدار (Regression)، المدرج التكراري (Histogram)، والملخص الإحصائي.
* **Table:** جدول القيم للدوال (بداية، نهاية، خطوة).
* **Equation:** حل المعادلة والأجهزة ورسم حلولها.

---

## 5. النودز الذكية (Smart Nodes Editor)

تُبنى واجهة المحرر باستخدام مكتبة `@xyflow/react` مع دعم العقد التالية:
* `Prompt` | `Calculator Expression` | `Graph Equation` | `Python File` | `Run Python` | `Matrix` | `Vector` | `Statistics` | `Condition` | `Variable` | `Result`

### آلية العمل:
عندما يطلب المستخدم: *"أنشئ Workflow يأخذ معادلة، يرسمها، يجد الجذور، ثم يكتب Python للتحقق منها"*:
1. يقوم الـ Agent بإنشاء مخطط العقد (Nodes & Edges).
2. **لا يتم التشغيل تلقائياً** قبل عرض العقد والروابط للمستخدم.
3. كل Node تمتلك مدخلات ومخرجات محددة صراحة باستخدام مكتبة `Zod`.
4. حماية المخطط من الحلقات غير المنتهية (Infinite Loops) وتحديد حد أقصى لعدد العقد وخطوات التنفيذ.

---

## 6. استراتيجية تقليل استهلاك الحصة (Token Optimization)

بدلاً من إرسال حالة التطبيق الكاملة، ينشئ الـ **Context Builder** حزمة بيانات مصغرة بحسب نوع المهمة:

* **سؤال حسابي:** التعبير الحسابي الحالي، قيمة `Ans` الأخيرة، وحدة الزوايا، والمتغيرات المسجلة فقط.
* **سؤال الرسم البياني (Graph):** قائمة المعادلات النشطة وحدود المحاور فقط.
* **سؤال بيئة Python:** الملف النشط حالياً والمخرجات الأخيرة فقط.
* **سؤال المصفوفات (Matrix):** المصفوفات المختارة فقط.
* **سؤال عام:** ملخص للمحادثة دون إرسال الحالة الكاملة للحاسبة.

### قواعد الضغط والاستهلاك:
1. الاحتفاظ بآخر **6 إلى 10 رسائل** فقط مع دمج الرسائل القديمة في ملخص نصي موجز.
2. إرسال الأدوات (Tools) المتعلقة بالقسم الحالي فقط لتخفيض الـ System Prompt Tokens.
3. عدم إرسال ملفات Python كاملة، بل يطلب النموذج قراءة الملف المحدد باستخدام أداة عند الحاجة.
4. استخدام الترقيعات (Patches / Diffs) عند تعديل الكود بدلاً من إعادة إرسال الملف كاملاً.
5. منع إجراء أكثر من دورة Agent واحدة (Agent Turn) للجلسة في الوقت نفسه.
6. تحديد حد أقصى لدورات استخدام الأدوات (Tool Loops) بمعدل **4 دورات لكل رسالة**.
7. استخدام التخزين المؤقت المحلي (Local Cache) لإجابات الأسئلة الثابتة التي لا تتغير.
8. إلغاء/إيقاف الطلبات فوراً عند إغلاق النافذة أو تغيير القسم.

---

## 7. إدارة حدود الاستهلاك والأخطاء (Quota Manager)

يمتلك الـ Quota Manager عدادات منفصلة لكل نموذج بالشكل التالي:

```typescript
type ModelQuotaState = {
  rpm: number;
  tpm: number;
  rpd: number;
  usedRequests: number;
  usedTokens: number;
  retryAfter: number;
  circuitState: 'CLOSED' | 'OPEN' | 'HALF_OPEN';
};
```

* **هامش الأمان:** يتم استخدام **75% فقط** من الحدود المعلنة كحد أقصى لتجنب الحظر.
* **عند استلام خطأ 429:**
  1. قراءة هيدر `retry-after`.
  2. تطبيق خوارزمية **Exponential Backoff with Jitter**.
  3. وضع الطلب في قائمة الانتظار (Queue).
  4. الانقاط للنموذج البديل (Fallback Model) **مرة واحدة فقط**.
  5. تفعيل مفتاح الحماية (Circuit Breaker) مؤقتاً في حال تكرار الخطأ.
  6. عرض رسالة واضحة للمستخدم تشرح حالة الضغط بدلاً من طباعة `[object Object]`.

---

## 8. معايير الأمان والحماية (Security Guardrails)

* **القراءة والمحادثة العادية:** تنفذ مباشرة دون اعتراض.
* **إضافة معادلة / تغيير وضع:** معاينة قصيرة مع إمكانية التراجع (Undo).
* **تعديل كود برمي:** عرض الفرق (Diff View) قبل الاعتماد.
* **حذف ملف / استبدال:** نافذة تأكيد (Dialog) تعرض اسم الملف كاملاً.
* **تشغيل كود Python:** يتم داخل **Pyodide Worker** معزول، يحتوي على زر إيقاف فوري (Stop Execution) وحد زمني للتنفيذ (Timeout).
* **فحص الكود:** استخدام `ast.parse` لفحص أكواد Python قبل الحفظ والتشغيل.
* **منع الوصول:** حظر تام لوصول الكود إلى نظام الملفات المحلي، الـ Shell، أو متغيرات البيئة الخاصة بالخادم.
* **التسجيل (Logging):** تسجيل أسماء الأدوات المستخدمة والأخطاء فقط، مع تشفير ومسح مفاتيح الـ API والأكواد الحساسة من السجلات.

---

## 9. هياكل الملفات المقترحة (Project Structure)

```text
├── app/
│   └── api/
│       └── agent/
│           └── turn/
│               └── route.ts
├── components/
│   └── agent/
│       ├── AgentPanel.tsx
│       ├── ToolApprovalDialog.tsx
│       ├── WorkflowCanvas.tsx
│       └── QuotaIndicator.tsx
├── hooks/
│   └── useCalculatorAgent.ts
├── lib/
│   ├── ai/
│   │   ├── gemini.ts
│   │   ├── model-router.ts
│   │   ├── quota-manager.ts
│   │   ├── context-builder.ts
│   │   └── prompts.ts
│   └── agent/
│       ├── tool-registry.ts
│       └── tools/
│           ├── calculator.ts
│           ├── graph.ts
│           ├── python.ts
│           └── workflow.ts
├── store/
│   └── agentStore.ts
└── types/
    └── agent.ts
```

---

## 10. خطة التنفيذ الموصى بها (Implementation Roadmap)

1. **المرحلة الأولى:** تثبيت الـ SDK الجديد `@google/genai` وتأمين الـ Route ومفتاح API في الخادم.
2. **المرحلة الثانية:** بناء الـ Model Registry والمقبول للتحديث الديناميكي.
3. **المرحلة الثالثة:** نقل حالات الحاسبة، Python، و Graph إلى واجهات Store قابلة للتحكم وسحب الـ Snapshots المصغرة.
4. **المرحلة الرابعة:** بناء سجل الأدوات (Tool Registry) ونظام الـ Function Calling.
5. **المرحلة الخامسة:** إضافة واجهات الموافقات والتراجعات (Diff & Approval Dialogs).
6. **المرحلة السادسة:** دمج بيئة تشغيل Python عبر Pyodide مع أدوات الفحص والأمان.
7. **المرحلة السابعة:** بناء محرر الـ Nodes الذكي باستعمال `@xyflow/react`.
8. **المرحلة الثامنة:** تطبيق الـ Quota Manager والـ Circuit Breaker ونظام البث (Streaming) التفاعلي.

---

## 11. معايير القبول (Acceptance Criteria)

- [ ] يستطيع المستخدم طلب حسابات معقدة ورؤية النواتج موثقة على الواجهة.
- [ ] يتمكن الـ Agent من التنقل بين الأقسام بمرونة دون إرسال الحالة الكاملة للتطبيق.
- [ ] القدرة على رسم عدة معادلات مع ضبط المحاور بدقة عبر الـ Agent.
- [ ] عرض الـ Diff قبل تعديل أية ملفات Python وتأكيد التشغيل عبر Pyodide Worker.
- [ ] بناء وتشغيل الـ Workflows عبر محرر الـ Nodes دون خطأ الحلقات المفرغة.
- [ ] عدم تنفيذ أي عملية حذف أو استبدال بدون موافقة صريحة من المستخدم.
- [ ] الالتزام بحدود الكوتا وإدارة أخطاء Gemini برسايل واضحة للمستخدم.